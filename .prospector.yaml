pyflakes:
  disable:
    # "Imported but unused" in __init__.py files
    # Unfortunately, Prospector currently doesn't per-file ignores.
    # Actual unused imports should still be caught by PyLint.
    - F401

pylint:
  # PyLint is run separately by Codacy
  run: false
